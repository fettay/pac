module.exports = {
    publicRuntimeConfig: {
      // Will be available on both server and client
      serverUrl: 'https://api.energie-durable.net',
    },
    serverRuntimeConfig: {
      
    }
  }